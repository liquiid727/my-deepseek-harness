# 2026-09-09 Implement：待裁决事项

- 状态：blocked / pending decisions
- 记录日期：2026-09-09
- 范围：Med Research Workspace 阶段 7–8 收尾与整体 DoD

## 结论

当前已完成的实现、测试、客户端构建和 profile 验证不回退。以下事项暂不自行实现：它们涉及 PRD/SPEC 冲突、Remote 公共契约或缺失的 DSH 上游依赖。收到对应裁决后，再按一个阶段一个阶段的顺序继续。

## 待裁决

### 1. Remote canonical namespace

SPEC §30 的汇总表写作 `medResearch/planQuery | search | get | papers`，但 SPEC §5/§6、PRD 和当前代码一致使用 `medLiterature` + `medPapers`（`getPaper`）。

- 当前决定：不添加别名，不同时暴露两套模型可见 surface。
- 需要决定：以 `medResearch` 为准并重命名，还是确认 §30 汇总表为笔误并保留当前 canonical surface。
- 证据：`docs/decisions/2026-09-09-remote-surface-spec-drift.md`。

### 2. Remote 列表读取契约

当前 UI 是 focus-driven，缺少项目论文、文档段落、项目证据、数据集和分析运行的列表读取方法。

- 候选接口：`medProjects/papers`、`medPapers/paragraphs`、`medEvidence/listForProject`、`medDatasets/list`、`medStatistics/runs`。
- 影响：列表视图、引用发现、Statistics 运行结果浏览无法完整接线。
- 需要决定：是否扩展 `@Remote` 与 `Med*Service` 接口，以及每个列表的过滤/分页契约。

### 3. P0 分析结果图表接线

P0 图表类型已按 PRD §26/§36 确定为 Histogram、Box Plot、Bar Chart、Scatter；当前已有无依赖 SVG 模板、profile 缺失值 Bar Chart 和 figure artifact 链，但没有分析结果的统一图表字段及 Remote/UI 结果展示契约。

- 需要决定：分析代码如何声明图表类型与聚合输入，结果 JSON 如何携带图表，客户端按何种 schema 渲染并关联 `analysisRunId`。
- 在此之前不把模板伪装成完整结果页。
- 证据：`docs/decisions/2026-09-09-p0-chart-types.md`。

### 4. Agent Mode 入口与允许列表

PRD §31 / SPEC §39 要求使用 `ctx.tools.restrict()` 动态允许列表，但没有定义用户切换入口；当前插件 apply 阶段也没有可作用于会话的 agent context。

- 需要决定：DSH 命令、设置项或会话动作作为切换入口。
- 需要确认：三种 mode 的 canonical 工具前缀列表及 `paper_get_content` 与 `paper_get_document` 的命名取舍。

### 5. 右栏席位上游依赖

`sidebar.right.pane.tab` 所需的 `@deepseek-ai/dsh-client-ui-sidebar-right` 及 dockkit 未发布到 npm，当前无法按 DSH 约定注册 Paper Reader / Evidence 详情右栏。

- 当前处理：引用通过 `med-papers` focus 打开，不自建 shell 或 URL 路由。
- 恢复条件：上游发布依赖，或明确 out-of-tree 的替代 slot / 类型契约。
- 证据：`docs/decisions/2026-09-08-phase7-client-ui.md`。

## 暂不宣称完成

在上述事项裁决前，不宣称以下整体目标已完成：

- Statistics DoD 的“分析结果图表 → 结果页”完整链路。
- 阶段 7 的列表驱动浏览与右栏阅读器。
- FR-22 Agent Mode 动态允许列表。
- PRD §35 / §36 的整体收尾。

## 已验证基线

- `pnpm run typecheck`：通过。
- `pnpm run test`：45 files / 253 passed + 1 skipped（macOS 内存强制断言）。
- `pnpm run verify:client`：通过。
- profile 安装、`dsh --profile med-research --dump-config` 与浏览器视图切换：已验证。
