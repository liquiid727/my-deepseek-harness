/**
 * Keyless end-to-end chains (PRD §36 Research DoD). The suites live under
 * `tests/`; this module exists so the package has a stable entry.
 * @module @medresearch/dsh-medical-e2e
 */

export {}
