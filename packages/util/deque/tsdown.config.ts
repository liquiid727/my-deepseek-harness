import { defineConfig } from 'tsdown'

/** Build the deque runtime from the declarations emitted by TypeScript. */
export default defineConfig({
  entry: ['lib/types/index.js'],
  outDir: 'lib',
  format: ['esm'],
  platform: 'node',
  target: 'es2024',
  fixedExtension: false,
  dts: false,
  clean: false,
})
