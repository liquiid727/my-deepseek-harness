# S01 — UI-HOME browser matrix (current build)

Binds: Spec 2.1.3 / `d345dc9f36a876e36a6f9ba144863acb970094cd56fb9ad2f764a92a4f7107a6`;
Test Design 2.0.1 / `79af3b71ef5c7f5e18b3c62e63882a3f4dc56b9c6a53e99afdf918d324c5ca4a`;
contract bundle `6703ad878703a2a02f7ae73a07d64b087cfc4c34f8c8222a80d813ce8ee4f428`;
revision `62ca9fac796ef751a4b84dc949791e8e83e14494` (clean tree, no dirty fingerprint).

## Binding stamp

- Source/worktree revision: `62ca9fac79` — the consolidated implementation commit;
  working tree clean (0 tracked/untracked source changes at capture time).
- Profile: `~/.dsh/profiles/med-research`, reinstalled from current source
  (`build:client` + `install-local-profile.mjs --force`); served at
  `http://127.0.0.1:3100/`.
- Model: `deepseek-v4.1-flash` (provider `ali`). Service: real project-service
  SQLite under `.med-run/`, project `住院时长队列` selected.
- Browser: ego-browser Chromium; viewport/DPR per shot below; locale `zh`;
  zoom 100%, deviceScaleFactor 1 (per `ui-acceptance.md`).
- Capture method: `Emulation.setDeviceMetricsOverride` to the three locked sizes,
  then `captureScreenshot`.

## Scenario: UI-HOME (Owner S01, asset `首页.png`)

Prerequisite data: existing persisted project `住院时长队列` (empty of
Papers/Evidence/Dataset/Run/Chart); no session→project binding on the chat lane.

Screenshots:
- `./screenshots/ui-home-1672x941-20260917.png` — landing, no current project.
- `./screenshots/ui-home-1440x900-20260917.png` — landing at 1440 (information
  order unchanged, no reflow to a different sequence).
- `./screenshots/ui-home-390x844-20260917.png` — narrow: single-column Hero/input,
  quick-entries wrap to a two-column grid, `项目概览`/cards stack.
- `./screenshots/ui-home-1672x941-project-counts-20260917.png` — project selected;
  five service counts visible.

## 1672×941 blocking assertions (from `ui-acceptance.md`)

- 当前 Project: header shows `当前项目 · 住院时长队列 · 进行中`; the selected project
  card carries a blue focus ring (current-project indicator). Present.
- Hero: `医学科研工作台 / 从一个临床问题，到可追溯的证据。` with the + brand mark. Present.
- 问题框与发送: research-question composer with send button, model chip
  `deepseek-v4.1-flash`, +/attach/`工作区内修改` controls. Input is the primary action.
- 五个快捷入口: `PubMed 检索 / 论文阅读 / 证据 / 统计分析 / 技能` — all five, one row. Present.
- 五项计数 = 服务: `0 文献论文 · 0 证据结论 · 0 数据集 · 0 分析任务 · 0 可视化图表`,
  `更新于 2026/9/17 15:22:24`; header note `来自项目服务的持久化计数`. Counts read live
  from the project service for the empty project.
- 三列 Research/Reader/Statistics 卡: present with descriptions and tags
  (`PubMed`/`检索计划`, `全文`/`原文定位`, `数据集`/`分析任务`) and 进入 arrows.
- 灵感栏: `研究灵感` with four rotating prompts and `换一批`.
- 项目区: `新建研究项目` form (`项目名称`/`研究问题`/`创建项目`) plus two project cards and
  `归档项目（可恢复）(1)`.

## Status / narrow-screen sub-assertions

- 空项目计数为 0: confirmed (all five counts 0 for `住院时长队列`).
- 390 卡单列、计数两列: cards render single-column; quick-entries wrap to a two-column
  grid; page stays within 390 width. Horizontal-overflow was not separately stress-tested.

## Limitations

- **This is the empty-project home state.** The UI-HOME populated prerequisite in
  `ui-acceptance.md` ("一个含通过服务写入的 Paper/Evidence/Dataset/Run/Chart" project,
  exercising non-zero counts and the 单域失败为未知 state) requires the S05 Runner
  chain to have written a real Run+Chart; not yet seeded, so those sub-assertions stay open.
- 200% zoom and the full same-run prototype-vs-actual per-region comparison table
  (font/spacing/color/density/occlusion) are not included in this pass.
- No cross-lane data: the chat model-chain (see `model-chain-smoke-20260917.md`) is a
  separate unbound session; the home counts reflect project-service state, not the chat.
